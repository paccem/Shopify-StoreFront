import { Box, IconButton, useColorMode } from '@chakra-ui/react'
import { MoonIcon, SunIcon } from '@chakra-ui/icon'


export default function ColorModeToggler() {
    // const { colorMode, toggleColorMode } = useColorMode()

    return (
        <Box>
            <MoonIcon />
        </Box>
    )
}
 